import type { SelectedOptionsType } from "@/types/options";

export default function correctText(text: string, options: SelectedOptionsType) {
	console.log("🚀 ~ correctText ~ options:", options);
	console.log("🚀 ~ text:", text);
}
